% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_gbif.R
\name{build_gbif}
\alias{build_gbif}
\title{Rebuild the GBIF backbone snapshot}
\usage{
build_gbif(
  version = format(Sys.Date(), "\%Y"),
  archive = NULL,
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{archive}{path to the GBIF backbone archive; downloaded if missing}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the GBIF backbone snapshot
}
\details{
GBIF supplies \code{canonicalName} -- the name without authorship --
alongside the full \code{scientificName}, so no name parsing is needed.  It
leaves \code{canonicalName} empty for names its parser cannot analyse, which
includes the sequence-derived identifiers GBIF carries in quantity (BOLD
BINs, UNITE species hypotheses, metagenome-assembled genomes) and hybrid
formulas; for those the \code{scientificName} is the name, and is used.

GBIF leaves \code{acceptedNameUsageID} empty on \code{accepted} and on \code{doubtful}
names alike, since it redirects neither.  Both therefore become their own
accepted name here, which is also what keeps GBIF's 40,895 synonyms of
doubtful names resolvable.
}
\examples{
\dontrun{
build_gbif("2026")
}
}
\seealso{
Other build:
\code{\link[=build_col]{build_col()}},
\code{\link[=build_itis]{build_itis()}},
\code{\link[=build_ncbi]{build_ncbi()}},
\code{\link[=build_ott]{build_ott()}}
}
\concept{build}
