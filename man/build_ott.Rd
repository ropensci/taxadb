% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_ott.R
\name{build_ott}
\alias{build_ott}
\title{Rebuild the Open Tree Taxonomy snapshot}
\usage{
build_ott(
  version = format(Sys.Date(), "\%Y"),
  archive = NULL,
  ott_version = "3.7.3",
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{archive}{path to the OTT release archive; downloaded if missing}

\item{ott_version}{the OTT release to build from, e.g. \code{"3.7.3"}}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the Open Tree Taxonomy snapshot
}
\details{
OTT ships \code{taxonomy.tsv} and \code{synonyms.tsv}, both delimited with
\verb{\\\\t|\\\\t}.  \code{synonyms.tsv} keys each synonym to the \code{uid} of the name it is
a synonym \emph{of}: OTT mints no identifier for the synonym itself, so those
rows carry a \code{NULL} \code{taxonID}, which the taxadb rules allow.

OTT no longer populates the \code{type} column of \code{synonyms.tsv} -- it is empty
for all 2.2 million rows in release 3.7.3 -- so a synonym is recorded as
\code{synonym} unless a type is given.

OTT publishes no vernacular names, so there is no \code{common} table for this
provider; \code{\link[=filter_common]{filter_common()}} warns accordingly.
}
\examples{
\dontrun{
build_ott("2026")
}
}
\seealso{
Other build:
\code{\link[=build_col]{build_col()}},
\code{\link[=build_fishbase]{build_fishbase()}},
\code{\link[=build_gbif]{build_gbif()}},
\code{\link[=build_itis]{build_itis()}},
\code{\link[=build_ncbi]{build_ncbi()}}
}
\concept{build}
