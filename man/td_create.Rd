% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/td_download.R
\name{td_create}
\alias{td_create}
\title{Create a local taxadb database}
\usage{
td_create(
  provider = getOption("taxadb_default_provider", "itis"),
  schema = c("dwc", "common"),
  version = latest_version(),
  overwrite = FALSE,
  lines = NULL,
  dbdir = NULL,
  db = td_connect()
)
}
\arguments{
\item{provider}{a character vector of provider(s) to download. See
\code{\link[=available_providers]{available_providers()}} for the providers published in a given version.}

\item{schema}{One of "dwc" (for Darwin Core data) or "common"
(for the Common names table.)}

\item{version}{Which version of the taxadb provider database should we use?
defaults to latest.  See \code{\link[=available_versions]{available_versions()}} for details.}

\item{overwrite}{passed to \code{\link[=td_download]{td_download()}}}

\item{lines}{deprecated, ignored.}

\item{dbdir}{deprecated, ignored.}

\item{db}{a connection to the taxadb database. See details.}
}
\value{
the local paths of the downloaded files, invisibly.
}
\description{
Superseded by \code{\link[=td_download]{td_download()}}.
}
\details{
\code{taxadb} no longer needs to import data before querying it:
tables are read directly from Parquet, streamed from remote storage or
from a local copy.  \code{td_create()} is retained as an alias for
\code{\link[=td_download]{td_download()}}, which installs a local copy.
}
\examples{
\donttest{
td_create("itis")
}
}
