% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_itis.R
\name{build_itis}
\alias{build_itis}
\title{Rebuild the ITIS snapshot}
\usage{
build_itis(
  version = format(Sys.Date(), "\%Y"),
  archive = NULL,
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{archive}{path to the ITIS SQLite archive; downloaded if missing}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the ITIS snapshot
}
\details{
ITIS distributes a SQLite database, which duckdb reads directly.
\code{n_usage} carries the accepted/synonym distinction under two vocabularies,
zoological (\code{valid}/\code{invalid}) and botanical (\code{accepted}/\verb{not accepted});
both map onto \code{accepted} and \code{synonym}.

ITIS assigns a TSN to synonyms as well as accepted names, so every row
here carries its own \code{taxonID} -- which makes ITIS the reference for the
taxadb rules checked by \code{\link[=td_validate]{td_validate()}}.
}
\examples{
\dontrun{
build_itis("2026")
}
}
\seealso{
Other build:
\code{\link[=build_col]{build_col()}},
\code{\link[=build_gbif]{build_gbif()}},
\code{\link[=build_ncbi]{build_ncbi()}}
}
\concept{build}
