% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/td_download.R
\name{td_download}
\alias{td_download}
\title{Install a local copy of a taxadb snapshot}
\usage{
td_download(
  provider = getOption("taxadb_default_provider", "itis"),
  schema = c("dwc", "common"),
  version = latest_version(),
  overwrite = FALSE,
  db = td_connect()
)
}
\arguments{
\item{provider}{a character vector of provider(s) to download. See
\code{\link[=available_providers]{available_providers()}} for the providers published in a given version.}

\item{schema}{One of "dwc" (for Darwin Core data) or "common"
(for the Common names table.)}

\item{version}{Which version of the taxadb provider database should we use?
defaults to latest.  See \code{\link[=available_versions]{available_versions()}} for details.}

\item{overwrite}{should we re-download files that are already present?
Default \code{FALSE}.}

\item{db}{a connection to the taxadb database. See details.}
}
\value{
the local paths of the downloaded files, invisibly.
}
\description{
Downloads the Parquet files for the requested provider(s) into
\code{\link[=taxadb_dir]{taxadb_dir()}}, so that subsequent queries read from local disk instead
of streaming from remote storage.
}
\details{
Streaming is fast enough for most interactive use and requires no
setup, so a local copy is optional.  Install one when you will make many
queries against the same table, when you need to work offline, or when you
want a snapshot pinned on disk for reproducibility.

Snapshots are large: the Darwin Core tables for \code{col} and \code{gbif} are each
several hundred MB.  Use \code{available_providers()} to see what is published,
and delete a local copy with \code{unlink(taxadb_dir(), recursive = TRUE)}.
}
\examples{
\donttest{
## Install a local copy of ITIS
td_download("itis")
}
}
