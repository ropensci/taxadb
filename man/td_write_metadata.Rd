% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{td_write_metadata}
\alias{td_write_metadata}
\title{Write the metadata published with a snapshot}
\usage{
td_write_metadata(
  version = format(Sys.Date(), "\%Y"),
  dir = build_dir(),
  repo = taxadb_repo(),
  db = td_connect()
)
}
\arguments{
\item{version}{the snapshot version to describe}

\item{dir}{the build output directory, see \code{\link[=build_dir]{build_dir()}}}

\item{repo}{the data repository the snapshot will be published to}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Writes \code{manifest.csv} and \code{README.md} into the snapshot directory, ready
to be uploaded alongside the Parquet files.
}
\details{
The README states what the tables are, what the schema means,
where each provider's data came from and under what licence, so that
someone who finds the data without the package can still use it.
}
\examples{
\dontrun{
td_write_metadata("2026")
}
}
