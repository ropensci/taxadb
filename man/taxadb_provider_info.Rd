% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{taxadb_provider_info}
\alias{taxadb_provider_info}
\title{Describe the taxonomic name providers}
\usage{
taxadb_provider_info(provider = taxadb_providers())
}
\arguments{
\item{provider}{one or more provider abbreviations; all by default}
}
\value{
a data.frame with one row per provider giving its \code{title}, \code{url},
the \code{source} its data is taken from, its \code{license} and a \code{citation}.
}
\description{
Describe the taxonomic name providers
}
\details{
Providers are not interchangeable.  \code{col}, \code{gbif} and \code{ott} are
synthesis projects that integrate other checklists, while \code{itis}, \code{ncbi},
\code{fb} and \code{slb} are primary authorities; the \code{synthesis} column records
which is which.  More importantly, providers disagree: the same name can
be accepted by one and a synonym of something else in another, so a name
resolved against one provider should not be mixed with names resolved
against another.  See \code{vignette("data-sources")}.

Redistribution terms differ too.  \code{fb} and \code{slb} are CC BY-NC, so those
two tables may not be used commercially.
}
\examples{
taxadb_provider_info()
taxadb_provider_info("col")$citation
}
