% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_col.R
\name{build_col}
\alias{build_col}
\title{Rebuild the Catalogue of Life snapshot}
\usage{
build_col(
  version = format(Sys.Date(), "\%Y"),
  archive = NULL,
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{archive}{path to the COL Darwin Core Archive; downloaded if missing}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the Catalogue of Life snapshot
}
\details{
COL publishes a Darwin Core Archive whose \code{scientificName} carries
the authorship -- \verb{Acanthocerataceae Crawford & Round}.  taxadb wants the
canonical name, since authorship abbreviations vary too much between
providers to match on.  COL also supplies \code{scientificNameAuthorship}
separately, so the canonical name is the one with that suffix removed.

COL marks accepted names by leaving \code{acceptedNameUsageID} empty, and
distinguishes \code{accepted} from \verb{provisionally accepted}; both are accepted
in the sense that matters here, that they are not a synonym of anything
else, so
both get \code{acceptedNameUsageID} set to their own \code{taxonID}.
}
\examples{
\dontrun{
build_col("2026")
}
}
\seealso{
Other build:
\code{\link[=build_fishbase]{build_fishbase()}},
\code{\link[=build_gbif]{build_gbif()}},
\code{\link[=build_itis]{build_itis()}},
\code{\link[=build_ncbi]{build_ncbi()}},
\code{\link[=build_ott]{build_ott()}}
}
\concept{build}
