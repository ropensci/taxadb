% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_ncbi.R
\name{build_ncbi}
\alias{build_ncbi}
\title{Rebuild the NCBI Taxonomy snapshot}
\usage{
build_ncbi(
  version = format(Sys.Date(), "\%Y"),
  archive = NULL,
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{archive}{path to the NCBI \code{taxdump.tar.gz}; downloaded if missing}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the NCBI Taxonomy snapshot
}
\details{
NCBI distributes \code{nodes.dmp} (the hierarchy) and \code{names.dmp}
(every name), in a format that claims to be tab-separated but delimits
fields with \verb{\\\\t|\\\\t}.

Every row of \code{names.dmp} carries the \emph{accepted} taxon's \code{tax_id}, whatever
the name's class: NCBI mints no separate identifier for a synonym.  So the
\verb{scientific name} rows become the accepted names, carrying a \code{taxonID} and
pointing \code{acceptedNameUsageID} at themselves, and every other name class
becomes a row for the same taxon with a \code{NULL} \code{taxonID} -- which the
taxadb rules permit, since there is no identifier to give.
}
\examples{
\dontrun{
build_ncbi("2026")
}
}
\seealso{
Other build:
\code{\link[=build_col]{build_col()}},
\code{\link[=build_itis]{build_itis()}}
}
\concept{build}
