% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_fishbase.R
\name{build_fishbase}
\alias{build_fishbase}
\title{Rebuild the FishBase or SeaLifeBase snapshot}
\usage{
build_fishbase(
  version = format(Sys.Date(), "\%Y"),
  provider = c("fb", "slb"),
  fb_version = NULL,
  dir = build_dir(),
  db = td_connect()
)
}
\arguments{
\item{version}{snapshot version to write, e.g. \code{"2026"}}

\item{provider}{\code{"fb"} for FishBase or \code{"slb"} for SeaLifeBase}

\item{fb_version}{which FishBase snapshot to build from, e.g. \code{"v26.07"}.
Defaults to the most recent published.}

\item{dir}{directory for build inputs and outputs}

\item{db}{a duckdb connection}
}
\value{
the paths written, invisibly
}
\description{
Rebuild the FishBase or SeaLifeBase snapshot
}
\details{
FishBase and SeaLifeBase share a schema, and both are already
published as Parquet alongside the taxadb snapshots, so this build reads
them over the network and downloads nothing.

FishBase numbers accepted species (\code{SpecCode}) and synonyms (\code{SynCode})
in two independent sequences, so the same integer means different things
in each: \code{SpecCode} 1 is \emph{Scyris indica} while \code{SynCode} 1 is \emph{Alausa
coerulea}.  Prefixing both as \code{FB:1} would make one identifier name two
taxa, which it did in the previously published table -- 20,295 FishBase
identifiers and 61,125 SeaLifeBase ones were ambiguous.

Only \code{SpecCode} is therefore used as the \code{taxonID}, and synonyms carry a
\code{NULL} one exactly as they do for NCBI and OTT.  Nothing is lost: the
\code{SynCode} is published in its own \code{synonymID} column.  A consequence is
that a synonym whose \code{SpecCode} is 0 -- not linked to any species record,
1,043 names in FishBase and 7,512 in SeaLifeBase -- has nothing to
resolve to and is dropped.

Classification comes from the \code{families} table.  FishBase covers only
fishes, so its phylum and kingdom are constant.  SeaLifeBase spans some
sixty phyla across several kingdoms and asserts no kingdom itself, so
\code{kingdom} is left empty there rather than inferred.

FishBase data is CC-BY-NC (fishbase.org).
}
\examples{
\dontrun{
build_fishbase("2026", provider = "fb")
}
}
\seealso{
Other build:
\code{\link[=build_col]{build_col()}},
\code{\link[=build_gbif]{build_gbif()}},
\code{\link[=build_itis]{build_itis()}},
\code{\link[=build_ncbi]{build_ncbi()}},
\code{\link[=build_ott]{build_ott()}}
}
\concept{build}
